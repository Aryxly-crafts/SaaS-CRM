[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Runs Next.js SSR, middleware, and server actions on Netlify Functions.
[[plugins]]
  package = "@netlify/plugin-nextjs"
